# Required configuration for Read the Docs
version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11" 
# Build documentation in the specified format
sphinx:
  configuration: docs/source/conf.py 

# Install necessary Python dependencies (if needed)
python:
  install:
    - requirements: requirements.txt 